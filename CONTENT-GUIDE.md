# 콘텐츠 교체 가이드

이 사이트는 **정식 오픈 전** 상태를 전제로 만들었습니다.
기획서 07-5의 **AC-01(오픈예정 상태와 실제 운영 상태를 명확히 구분)** 과
**AC-02(승인된 가격 데이터만 공개)** 원칙에 따라, 확정되지 않은 정보는
임의로 지어내지 않고 **"확정 후 공개"** 로 표기해 두었습니다.

아래는 실제 값이 정해졌을 때 바꿔야 할 위치 목록입니다.
우선순위 순으로 정리했으니 위에서부터 처리하시면 됩니다.

---

## A. 오픈 즉시 필요 (P0)

### A-1. 법인 정보 — 모든 페이지 푸터

| 위치 | 현재 문구 | 교체할 내용 |
|---|---|---|
| 전체 `.html` 푸터 `.ft__biz` | 상호·사업자등록번호·주소·대표자·통신판매업 신고번호는 법인 등록 완료 후 게재합니다. | 실제 법인 정보 |
| 전체 `.html` 푸터 `.ft__biz` | 고객센터 대표번호와 운영시간은 1호점 오픈에 맞춰 안내드립니다. | 대표번호, 운영시간 |

> 14개 파일에 동일하게 들어 있습니다. `tools/build-pages.py` 의 `footer()` 함수를 고치고
> `python3 build-pages.py` 로 한 번에 재생성하는 편이 안전합니다.

**전자상거래법상 표시 의무 항목** — 오픈 전 반드시 채워야 합니다.

```
상호 / 대표자 / 사업자등록번호 / 통신판매업 신고번호 /
사업장 주소 / 대표 전화 / 이메일 / 개인정보 보호책임자
```

### A-2. 1호점 정보

| 파일 | 위치 | 현재 | 교체 |
|---|---|---|---|
| `index.html` | 지점 안내 섹션 `<dl>` | 지역 · 오픈일 → `확정 후 공개` | 실제 지역, 오픈일 |
| `stores.html` | 1호점 카드 `<dl>` | 지역 · 오픈일 → `확정 후 공개` | 실제 주소, 오픈일 |
| `store-detail.html` | 상단 `.kv` 4칸 | 상태 · 지역 · 오픈일 · 운영시간 | 실제 값 |
| `store-detail.html` | 지점 공지 `.empty` | 아직 등록된 공지가 없습니다 | 첫 공지 내용 |

지점 상세에는 아래 항목을 추가로 넣어 주세요 (기획서 ORM-005 기준).

- 주소, 층별 안내, 운영시간, 휴무일
- 주차 대수·요금·진입로 안내
- 지점 대표번호
- 지도 / 길찾기 링크 (ORM-004)

### A-3. 상단 안내바 문구

| 파일 | 위치 | 현재 |
|---|---|---|
| 전체 `.html` | `.topbar` | `오픈 준비 중` + `1호점 오픈일은 확정되는 대로…` |

오픈 후에는 배지를 `영업 중` 으로, 문구를 운영시간이나 진행 중인 행사 안내로 바꾸면 됩니다.
`tools/build-pages.py` 의 `header()` 함수에서 관리합니다.

### A-4. 오픈 준비 현황 — **AC-01 직결**

`index.html` 의 `#progress` 섹션. 5단계 각각의 상태 배지를 실제 진행에 맞춰 갱신합니다.

| 단계 | 현재 상태 | 상태 클래스 |
|---|---|---|
| 브랜드·매장 콘셉트 확정 | 완료 | `prep__it--done` + `badge--go` |
| 출점 후보지 검토 | 진행 중 | `prep__it--now` + `badge--wait` |
| 공급사 입점·상품 소싱 | 제안 접수 중 | `prep__it--now` + `badge--wait` |
| 매장 시공·집기 반입 | 예정 | (없음) + `badge` |
| 채용·시범 운영·오픈 | 예정 | (없음) + `badge` |

> **이 섹션은 사실과 다르면 안 됩니다.** 현재 값은 예시 배치이므로
> 실제 진행 상황을 확인한 뒤 첫 배포 전에 반드시 맞춰 주세요.

---

## B. 오픈에 맞춰 필요 (P1)

### B-1. 가격 — **AC-02 직결**

현재 사이트에는 **실제 판매가가 한 건도 없습니다.** 의도된 상태입니다.

| 파일 | 위치 | 내용 |
|---|---|---|
| `index.html` | `.ptag` 가격표 예시 | `0,000원` 은 **표기 방식 설명용** 입니다. 실제 상품 가격으로 바꾸지 마세요. |
| `index.html` | 가격표 아래 `.note` | "오픈 전이라 실제 판매가는 아직 공개하지 않습니다" → 오픈 후 삭제 |

실제 가격을 노출할 때는 기획서 원칙을 지켜 주세요.

- PIM / 가격 승인 데이터에서 넘어온 값만 게시 (원천 DB 직접 노출 금지)
- 판매가 + 회원가 + 단위가격 + 행사 기간을 **함께** 표기
- 행사 종료일이 지나면 자동으로 정상가 복귀

### B-2. 특가·전단

| 파일 | 위치 | 현재 |
|---|---|---|
| `deals.html` | 상단 `.note` | 지금은 진행 중인 행사가 없습니다 |
| `deals.html` | 전단 섹션 버튼 | `전단 준비 중` (비활성) |

전단이 준비되면 회차별 다운로드 목록으로 교체합니다. 각 항목에 **행사 기간을 반드시 표기**하세요.

### B-3. 멤버십 정책

| 파일 | 위치 | 현재 |
|---|---|---|
| `membership.html` | 상단 `.note` | 혜택과 가입 조건은 아직 확정 전입니다 |
| `membership.html` | `준비 중인 혜택` 4개 카드 | 방향성만 기술 |

정책 확정 후 가입 조건, 연회비 유무, 혜택 적용 기준을 구체적으로 적고 `.note` 를 삭제합니다.

### B-4. 취급 품목

| 파일 | 위치 | 현재 |
|---|---|---|
| `category.html` | 상단 `.note` | 취급 품목은 오픈 전 소싱 결과에 따라 조정됩니다 |
| `category.html` | 통로 01~05 표 | 카테고리별 기본 구성 계획 |

소싱이 끝나면 실제 취급 품목으로 교체하고 `.note` 를 지웁니다.

### B-5. 결제·교환·환불 기준

| 파일 | 위치 | 현재 |
|---|---|---|
| `faq.html` | "어떤 결제 수단을 쓸 수 있나요?" | 준비 중이라고 표기 |
| `faq.html` | "교환과 환불은 어떻게 하나요?" | 세부 기준은 오픈 전 게시 예정이라고 표기 |

확정된 기준은 **홈페이지와 매장 게시물에 같은 문장**으로 올려야 합니다.

### B-6. 고객센터 연락처

| 파일 | 위치 | 현재 |
|---|---|---|
| `faq.html` | 문의 유형별 창구 표 | 지점 대표번호 → `오픈 시 안내` |
| `faq.html` | 하단 `.note` | 대표번호·운영시간 게재 예정 |
| `privacy.html` | 8. 개인정보 보호책임자 | 확정 후 게재 |

---

## C. 법무 검토 필요 (P0, 별도 트랙)

`privacy.html` 과 `terms.html` 상단에 **"법인 등록 정보 확정 전 게시본"** 안내가 붙어 있습니다.
법무 검토가 끝나면 이 `.note` 블록을 삭제하세요.

### 개인정보처리방침 확정 항목

| 항목 | 현재 |
|---|---|
| 4. 개인정보 처리의 위탁 | 위탁 업무만 나열, 수탁사 상호 미기재 → **실제 수탁사 확정 후 기재 필수** |
| 8. 개인정보 보호책임자 | 성명·직위·연락처 → `확정 후 게재` |
| 시행일 | `홈페이지 정식 오픈일 (확정 후 게재)` |

> 문자·이메일 발송 대행사, 호스팅 사업자(Cloudflare)를 수탁사로 명시해야 합니다.

### 이용약관 확정 항목

| 항목 | 현재 |
|---|---|
| 시행일 | `홈페이지 정식 오픈일 (확정 후 게재)` |
| 제1조 회사 정의 | 정식 상호 미확정 |

매장 이용·상품 판매에 관한 약관(교환·환불 포함)은 **별도 문서**로 추가 게시가 필요합니다.

---

## D. 지금 상태로 두어도 되는 것

아래는 확정 정보가 아니어도 문제없는 항목입니다. 참고용으로만 적어 둡니다.

| 항목 | 이유 |
|---|---|
| 2호점·3호점 카드 | `검토 중` 상태 자체가 사실 전달이므로 그대로 두어도 됩니다 |
| 매장 이미지 전체 | 설계 단계 이미지임을 각 섹션에 명시했습니다. 시공 후 실사로 교체하면 됩니다 |
| `store-detail.html` 구역 안내 표 | 설계 확정 내용이라 오픈 전에도 공개 가능합니다 |
| 브랜드 표기 기준 (`brand.html`) | 확정된 디자인 시스템 값입니다 |
| 가격표 표기 원칙 (`index.html`) | 정책 설명이므로 오픈 후에도 유지 권장 |

---

## E. 문구 일괄 검색 키워드

교체가 필요한 곳을 빠르게 찾으려면 아래 단어로 검색하세요.

```bash
grep -rn "확정 후 공개"     *.html   # 지점·오픈일 등 미확정 값
grep -rn "확정 후 게재"     *.html   # 법무 관련 미확정 값
grep -rn "준비 중"          *.html   # 미오픈 기능 안내
grep -rn "0,000"            *.html   # 가격표 예시 숫자
grep -rn "orange-market.pages.dev" . # 임시 도메인
```

---

## F. 교체 후 재확인

콘텐츠를 바꾼 뒤에는 아래를 확인해 주세요.

1. **홈 `#progress` 의 단계 상태가 실제와 같은가** (AC-01)
2. **게시한 가격이 모두 승인된 값인가** (AC-02)
3. 푸터 법인 정보가 모든 페이지에 동일하게 들어갔는가
4. 개인정보처리방침의 수집 항목이 실제 폼 항목과 일치하는가
5. 모바일 화면에서 새 문구가 넘치거나 잘리지 않는가

`README.md` 7장의 배포 전 확인 목록도 함께 점검하시면 좋습니다.
